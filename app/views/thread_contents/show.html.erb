<p id="notice"><%= notice %></p>

<div>
  <p style="font-size:20px"> <b><%= @thread_content.content %></b> 
  	<%= link_to 'add post', new_post_path if can? :create, Post %></p>
</div>

<% if cannot? :create , Post %>

<% else %>
	<p>
	<%= link_to 'Edit', edit_thread_content_path(@thread_content) if can? :edit, Post %>
	<%= link_to 'Back', topics_path %>
	<p>
<% end %>

<%= @thread_content.posts.each do |post| %>
   <p style="font-size:14px"> <b><%= post.content %></b></p>
<% end %>
