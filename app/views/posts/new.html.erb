<% if cannot? :create , Post %>
	<span class="permission-message">You aren't allowed to create new post.</span>
<% else %>
	<h1>New Post</h1>

	<%= render 'form' %>

	<%= link_to 'Back', posts_path %>
<% end %>
